#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first operand
 * @b: second operand
 *
 * Description: multiplies the two operands @a * @b
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
